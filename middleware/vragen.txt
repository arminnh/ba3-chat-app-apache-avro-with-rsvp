Messages:
	Objecten in een message object of zelf een formaat definieren met 1 lange string die alls beschrijft

3.3.1:
	placeholder for the implementation of the handling of an incoming request. 
	
4.1 Communication module:
	constructor ipv create functie
	zijn getters en setters nodig
	initialisatie van socket



Wordt er een nieuwe thread aangemaakt voor een nieuwe remote invocation	
//Wanneer stopt de thread, na 1 message

	
Implement the initialisation  of  the  Socket  towards  the  server-side  RMI  platform  you  created  
earlier and  the CommunicationModule class  itself.   The  thread  may  stop  execution after reading 
one response message from the server.